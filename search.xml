<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Qt中的正则表达式</title>
      <link href="/2019/12/08/Qt%E4%B8%AD%E7%9A%84%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/"/>
      <url>/2019/12/08/Qt%E4%B8%AD%E7%9A%84%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
      
        <content type="html"><![CDATA[<h2 id="QRegularExpression"><a href="#QRegularExpression" class="headerlink" title="QRegularExpression"></a>QRegularExpression</h2><p>QRegularExpression主要由pattern string和pattern options集合组成，构造一个对象，可以使用</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;a pattern&quot;);</span><br></pre></td></tr></table></figure><p>或</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re;</span><br><span class="line">re.setPattern(&quot;another pattern&quot;);</span><br></pre></td></tr></table></figure><p>如果想在pattern string中包含’&#39;，则需要使用转义”\“，如</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// matches two digits followed by a space and a word</span><br><span class="line">QRegularExpression re(&quot;\\d\\d \\w+&quot;);</span><br><span class="line"></span><br><span class="line">// matches a backslash</span><br><span class="line">QRegularExpression re2(&quot;\\\\&quot;);</span><br></pre></td></tr></table></figure><h3 id="Pattern-Options"><a href="#Pattern-Options" class="headerlink" title="Pattern Options"></a>Pattern Options</h3><p>在构造函数中设置</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">// 设置匹配大小写</span><br><span class="line">// matches &quot;Qt rocks&quot;, but also &quot;QT rocks&quot;, &quot;QT ROCKS&quot;, &quot;qT rOcKs&quot;, etc.</span><br><span class="line">QRegularExpression re(&quot;Qt rocks&quot;, QRegularExpression::CaseInsensitiveOption);</span><br></pre></td></tr></table></figure><p>通过setPatternOptions设置</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;^\\d+$&quot;);</span><br><span class="line">// </span><br><span class="line">re.setPatternOptions(QRegularExpression::MultilineOption);</span><br><span class="line">// re matches any line in the subject string that contains only digits (but at least one)</span><br></pre></td></tr></table></figure><p>通过patternOptions获取当前设置</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re = QRegularExpression(&quot;^two.*words$&quot;, QRegularExpression::MultilineOption | QRegularExpression::DotMatchesEverythingOption);</span><br><span class="line"></span><br><span class="line">QRegularExpression::PatternOptions options = re.patternOptions();</span><br><span class="line">// options == QRegularExpression::MultilineOption |QRegularExpression::DotMatchesEverythingOption</span><br></pre></td></tr></table></figure><h3 id="Match-Type-and-Match-Options"><a href="#Match-Type-and-Match-Options" class="headerlink" title="Match Type and Match Options"></a>Match Type and Match Options</h3><p>match和globalMatch函数的最后两个参数是match type和match options，match type指定了匹配算法，默认是NormalMatch。</p><h3 id="Normal-Matching"><a href="#Normal-Matching" class="headerlink" title="Normal Matching"></a>Normal Matching</h3><p>要执行一次match，只需要调用match函数，并传递要匹配的字符串即可，返回值是一个QRegularExpressionMatch对象。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">// match two digits followed by a space and a word</span><br><span class="line">QRegularExpression re(&quot;\\d\\d \\w+&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;abc123 def&quot;);</span><br><span class="line">bool hasMatch = match.hasMatch(); // true</span><br></pre></td></tr></table></figure><p>如果match成功，通过QRegularExpressionMatch对象可以获取到匹配元素：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;\\d\\d \\w+&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;abc123 def&quot;);</span><br><span class="line">if (match.hasMatch()) &#123;</span><br><span class="line">    QString matched = match.captured(0); // matched == &quot;23 def&quot;</span><br><span class="line">    // ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以传递一个偏移值，相对于搜索串的偏移，如下，”12 abc”不会被搜索到，因为是从搜索串的偏移1开始搜索：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;\\d\\d \\w+&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;12 abc 45 def&quot;, 1);</span><br><span class="line">if (match.hasMatch()) &#123;</span><br><span class="line">    QString matched = match.captured(0); // matched == &quot;45 def&quot;</span><br><span class="line">    // ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Extracting-captured-substrings"><a href="#Extracting-captured-substrings" class="headerlink" title="Extracting captured substrings"></a>Extracting captured substrings</h3><p>captured(int nth = 0)函数能够返回匹配组的第nth个对象：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;^(\\d\\d)/(\\d\\d)/(\\d\\d\\d\\d)$&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;08/12/1985&quot;);</span><br><span class="line">if (match.hasMatch()) &#123;</span><br><span class="line">    QString day = match.captured(1); // day == &quot;08&quot;</span><br><span class="line">    QString month = match.captured(2); // month == &quot;12&quot;</span><br><span class="line">    QString year = match.captured(3); // year == &quot;1985&quot;</span><br><span class="line">    // ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>匹配组的索引是从开始的，索引为0表示完全匹配pattern的字符串。<br>capturedStart(int nth = 0)和capturedEnd(int nth = 0)可以返回匹配字符串在搜索串中的位置，匹配串的长度即为 和capturedEnd - capturedStart：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;abc(\\d+)def&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;XYZabc123defXYZ&quot;);</span><br><span class="line">if (match.hasMatch()) &#123;</span><br><span class="line">    int startOffset = match.capturedStart(1); // startOffset == 6</span><br><span class="line">    int endOffset = match.capturedEnd(1); // endOffset == 9</span><br><span class="line">    // ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以给每个匹配项命名：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;^(?&lt;date&gt;\\d\\d)/(?&lt;month&gt;\\d\\d)/(?&lt;year&gt;\\d\\d\\d\\d)$&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;08/12/1985&quot;);</span><br><span class="line">if (match.hasMatch()) &#123;</span><br><span class="line">    QString date = match.captured(&quot;date&quot;); // date == &quot;08&quot;</span><br><span class="line">    QString month = match.captured(&quot;month&quot;); // month == &quot;12&quot;</span><br><span class="line">    QString year = match.captured(&quot;year&quot;); // year == 1985</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Global-Matching-搜索全部"><a href="#Global-Matching-搜索全部" class="headerlink" title="Global Matching// 搜索全部"></a>Global Matching// 搜索全部</h3><p>如下例子匹配所有单词，其中，返回值i指向的是第一个匹配项前的位置：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;(\\w+)&quot;);</span><br><span class="line">QRegularExpressionMatchIterator i = re.globalMatch(&quot;the quick fox&quot;);</span><br></pre></td></tr></table></figure><p>globalMatch函数的返回值是一个QRegularExpressionMatchIterator对象，通过它能够获取匹配结果，hasNext为true表示还有不止一个结果，next返回下一个结果集：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">QStringList words;</span><br><span class="line">while (i.hasNext()) &#123;</span><br><span class="line">    QRegularExpressionMatch match = i.next();</span><br><span class="line">    QString word = match.captured(1);</span><br><span class="line">    words &lt;&lt; word;</span><br><span class="line">&#125;</span><br><span class="line">// words contains &quot;the&quot;, &quot;quick&quot;, &quot;fox&quot;</span><br></pre></td></tr></table></figure><h3 id="Partial-Matching-部分匹配"><a href="#Partial-Matching-部分匹配" class="headerlink" title="Partial Matching// 部分匹配"></a>Partial Matching// 部分匹配</h3><p>部分匹配是指搜索串已到了结尾，但还需要更多字符串去达到完全匹配的结果。</p><p>当部分匹配找到的时候，hasMatch返回false且hasPartialMatch返回true。同时可以通过captured(0)获取到部分匹配的字符串。</p><p>注意：当指定部分匹配的时候，也会同时进行完全匹配。如果有完全匹配，则hasMatch返回true而hasPartialMatch返回false。但不会存在两者同时为true的情况。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">re.setPattern(&quot;(\\dabc\\d)&quot;);</span><br><span class="line">match = re.match(&quot;1abc&quot;, 0, QRegularExpression::PartialPreferCompleteMatch);</span><br><span class="line">qDebug()&lt;&lt;match.hasMatch()&lt;&lt;match.hasPartialMatch();// false true</span><br><span class="line">words.clear();</span><br><span class="line">if (match.hasPartialMatch()) &#123;</span><br><span class="line">    QString word = match.captured(0);</span><br><span class="line">    words &lt;&lt; word;</span><br><span class="line">&#125;</span><br><span class="line">qDebug()&lt;&lt;words;// 1abc</span><br></pre></td></tr></table></figure><h3 id="Validating-user-input"><a href="#Validating-user-input" class="headerlink" title="Validating user input"></a>Validating user input</h3><p>假设我们要验证用户输入的日期是否符合某个格式，可以使用这样的匹配模式：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">^(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec) \d\d?, \d\d\d\d$</span><br></pre></td></tr></table></figure><p>以下使用的都是PartialPreferCompleteMatch模式。</p><p>只有部分匹配的情况：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">QString pattern(&quot;^(Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec) \\d\\d?, \\d\\d\\d\\d$&quot;);</span><br><span class="line">QRegularExpression re(pattern);</span><br><span class="line"></span><br><span class="line">QString input(&quot;Jan 21,&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(input, 0, QRegularExpression::PartialPreferCompleteMatch);</span><br><span class="line">bool hasMatch = match.hasMatch(); // false</span><br><span class="line">bool hasPartialMatch = match.hasPartialMatch(); // true</span><br></pre></td></tr></table></figure><p>有全部匹配的情况：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">QString input(&quot;Dec 8, 1985&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(input, 0, QRegularExpression::PartialPreferCompleteMatch);</span><br><span class="line">bool hasMatch = match.hasMatch(); // true</span><br><span class="line">bool hasPartialMatch = match.hasPartialMatch(); // false</span><br></pre></td></tr></table></figure><p>另一种匹配模式的表现：<br>有全部匹配的情况：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;abc\\w+X|def&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;abcdef&quot;, 0, QRegularExpression::PartialPreferCompleteMatch);</span><br><span class="line">bool hasMatch = match.hasMatch(); // true</span><br><span class="line">bool hasPartialMatch = match.hasPartialMatch(); // false</span><br><span class="line">QString captured = match.captured(0); // captured == &quot;def&quot;</span><br></pre></td></tr></table></figure><p>abc\w+X部分匹配，而def全部匹配。</p><p>如果有多个部分匹配（但没有全部匹配），则第一个找到的将会返回：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;abc\\w+X|defY&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;abcdef&quot;, 0, QRegularExpression::PartialPreferCompleteMatch);</span><br><span class="line">bool hasMatch = match.hasMatch(); // false</span><br><span class="line">bool hasPartialMatch = match.hasPartialMatch(); // true</span><br><span class="line">QString captured = match.captured(0); // captured == &quot;abcdef&quot;</span><br></pre></td></tr></table></figure><h3 id="Incremental-multi-segment-matching"><a href="#Incremental-multi-segment-matching" class="headerlink" title="Incremental/multi-segment matching"></a>Incremental/multi-segment matching</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;abc|ab&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;ab&quot;, 0, QRegularExpression::PartialPreferFirstMatch);</span><br><span class="line">bool hasMatch = match.hasMatch(); // false</span><br><span class="line">bool hasPartialMatch = match.hasPartialMatch(); // true</span><br></pre></td></tr></table></figure><p>当第一个abc匹配模式查找到的时候即返回，此时为部分匹配。</p><p>还有两个更加特殊的例子：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression re(&quot;abc(def)?&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;abc&quot;, 0, QRegularExpression::PartialPreferFirstMatch);</span><br><span class="line">bool hasMatch = match.hasMatch(); // false</span><br><span class="line">bool hasPartialMatch = match.hasPartialMatch(); // true</span><br><span class="line"></span><br><span class="line">QRegularExpression re(&quot;(abc)*&quot;);</span><br><span class="line">QRegularExpressionMatch match = re.match(&quot;abc&quot;, 0, QRegularExpression::PartialPreferFirstMatch);</span><br><span class="line">bool hasMatch = match.hasMatch(); // false</span><br><span class="line">bool hasPartialMatch = match.hasPartialMatch(); // true</span><br></pre></td></tr></table></figure><p>以上例子是因为?和*都是greedy的（贪心），它们会尽可能匹配一个或多个，所以当找到abc的时候，它们会接着查找，但此时已到达搜索串结尾，所以返回的是部分匹配。</p><h3 id="Error-Handling"><a href="#Error-Handling" class="headerlink" title="Error Handling"></a>Error Handling</h3><p>可以通过QRegularExpression的isValid和errorString函数获取错误信息：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">QRegularExpression invalidRe(&quot;(unmatched|parenthesis&quot;);</span><br><span class="line">if (!invalidRe.isValid()) &#123;</span><br><span class="line">    QString errorString = invalidRe.errorString(); // errorString == &quot;missing )&quot;</span><br><span class="line">    int errorOffset = invalidRe.patternErrorOffset(); // errorOffset == 22</span><br><span class="line">    // ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果QRegularExpression无效，则match函数返回的QRegularExpressionMatch对象也无效。</p><h3 id="Unsupported-Perl-compatible-Regular-Expressions-Features"><a href="#Unsupported-Perl-compatible-Regular-Expressions-Features" class="headerlink" title="Unsupported Perl-compatible Regular Expressions Features"></a>Unsupported Perl-compatible Regular Expressions Features</h3><p>Qt未完全支持Perl的正则表达式语法，可能会在未来版本中添加。</p><h3 id="Notes-for-QRegExp-Users"><a href="#Notes-for-QRegExp-Users" class="headerlink" title="Notes for QRegExp Users"></a>Notes for QRegExp Users</h3><p>与QRegExp的差异，略。</p><p>推荐新版本都使用QRegularExpression。</p><h2 id="QRegExp"><a href="#QRegExp" class="headerlink" title="QRegExp"></a>QRegExp</h2><p>Qt中的正则表达式。</p><p>正则表达式由表达式、数量词和断言组成。</p><p>表达式，如单个字符’x’或’5’或某个字符集合，如[A-Z]。</p><p>数量词，指定了某个表达式出现的次数，如x{1,1}表示刚刚好出现一次x，x{1,5}则表示x最少出现一次，最多出现5次。</p><p>注意，正则表达式最好不要用来搜索配对括号或标签。因为<b>配对的是</b>，但在<b>bold <b>bolder</b></b>中，第一个<b>应该是跟第二个</b>配对。</p><p>在正则表达式中，^和$分别表示搜索字符串的开始和结尾。如搜索串应只包含匹配项，不包含其他字符。</p><p>假设我们要匹配0~99，要匹配刚好一个数字的话，是[0-9]{1,1}，那如果要匹配两个数字的话，则是[0-9]{1,2}，如果像表示匹配串是整个搜索串的话，则可以写成^ [0-9]{1,2}$。</p><p>实现同样的功能，正则表达式可以有不同的写法。比如[0-9]，也可以写为\d。数量词可以使用表达式本身表示，如x{1,1}，可以写为x。要匹配0~99的话，也可以写成^ \d{1,2}$，同样可以写为^\d\d?$，因为?是数量词{1,2}的缩写，表示出现0或1次。?表示一个表达式时可选的。</p><p>如果我们想要匹配 ‘mail’ or ‘letter’ or ‘correspondence’，但不匹配包含它们的单词。我们可以使用’mail|letter|correspondence’，但这个同样会匹配到’email’, ‘mailman’, ‘mailer’, and ‘letterbox’。为了避免这种情况，我们需要指定搜索词的边界，指定单词的边界，可以使用\b标签，如\b(mail|letter|correspondence)\b。\b表示的是单词的位置，单词的边界是任何非词字符，如空格，换行，或者字符串的开头或结尾。</p><p>如果我们想要将’&amp;’替换为html entity ‘&amp;amp’，如果简单使用正则表达式’&amp;’，则会匹配到原本已经转换过的&amp;amp，这不是我们想要的。此时，可以使用负向后行断言，’(?!__)’，所以可以写成’&amp;(?!amp;)’，就能够匹配到那些不是不是&amp;amp中的&amp;。</p><h3 id="Characters-and-Abbreviations-for-Sets-of-Characters"><a href="#Characters-and-Abbreviations-for-Sets-of-Characters" class="headerlink" title="Characters and Abbreviations for Sets of Characters"></a>Characters and Abbreviations for Sets of Characters</h3><table><thead><tr><th>Element</th><th>Meaning</th></tr></thead><tbody><tr><td>c</td><td>字符c</td></tr><tr><td>\c</td><td>字符c</td></tr><tr><td>\a</td><td>ASCII bell (BEL, 0x07)</td></tr><tr><td>\f</td><td>ASCII form feed (FF, 0x0C)</td></tr><tr><td>\n</td><td>ASCII line feed (LF, 0x0A, Unix newline).</td></tr><tr><td>\r</td><td>ASCII carriage return (CR, 0x0D).</td></tr><tr><td>\t</td><td>ASCII horizontal tab (HT, 0x09).</td></tr><tr><td>\v</td><td>ASCII vertical tab (VT, 0x0B).</td></tr><tr><td>\xhhhh</td><td>Unicode character corresponding to the hexadecimal number hhhh (between 0x0000 and 0xFFFF).</td></tr><tr><td>\0ooo (i.e., \zero ooo)</td><td>ASCII/Latin1 character for the octal number ooo (between 0 and 0377).</td></tr><tr><td>. (dot)</td><td>任意字符，包括换行符</td></tr><tr><td>\d</td><td>数字</td></tr><tr><td>\D</td><td>非数字</td></tr><tr><td>\s</td><td>空格字符</td></tr><tr><td>\S</td><td>非空格字符</td></tr><tr><td>\w</td><td>word字符，包括字母、数字、下划线</td></tr><tr><td>\W</td><td>非word字符</td></tr><tr><td>\s</td><td>空格字符</td></tr><tr><td>\S</td><td>非空格字符</td></tr></tbody></table><h3 id="Sets-of-Characters"><a href="#Sets-of-Characters" class="headerlink" title="Sets of Characters"></a>Sets of Characters</h3><p>[]中括号表示一个集合，包含在里面的表达式都会进行匹配。但有两种特殊情况：</p><p>中括号内的^表示不包括，比如[abc]表示匹配a或b或c，但[^abc]就表示包含除了a和b和c以外的所有字符。</p><p>中括号的-表示一个连续的序列，比如[W-Z]表示W或X或Y或Z。</p><h3 id="Quantifiers"><a href="#Quantifiers" class="headerlink" title="Quantifiers"></a>Quantifiers</h3><p>E代表表达式，表达式可以是一个字符，或一个字符集的缩略表示，或者中括号表示的字符集，或者用括号包含的表达式。</p><table><thead><tr><th>表达式</th><th>备注</th></tr></thead><tbody><tr><td>E?</td><td>E出现0次或1次，E是可选的。跟E{0,1}是一样的，如，dents?能匹配到’dent’或’dents’.</td></tr><tr><td>E+</td><td>E出现一次或多次，跟E{1,}一样，如，0+能匹配到 ‘0’, ‘00’, ‘000’等等.</td></tr><tr><td>E*</td><td>E出现0次或多次，跟E{0,}是一样的.</td></tr><tr><td>E{n}</td><td>E出现正好n次.</td></tr><tr><td>E{n,}</td><td>E出现最少n次.</td></tr><tr><td>E{,m}</td><td>E出现最多m次.</td></tr><tr><td>E{n,m}</td><td>E出现最少n次，最多m次.</td></tr></tbody></table><p>注意，在使用数量词的时候，最好使用括号括起来，比如，tag+匹配包含那些ta和至少出现一个g的。而(tag)+匹配最少出现一个tag的。</p><h3 id="Capturing-Text"><a href="#Capturing-Text" class="headerlink" title="Capturing Text"></a>Capturing Text</h3><p>略。</p><h3 id="Assertions"><a href="#Assertions" class="headerlink" title="Assertions"></a>Assertions</h3><table><thead><tr><th>表达式</th><th>备注</th></tr></thead><tbody><tr><td>^</td><td>表示字符串的起始位置，如果想匹配’^’字符，则需要使用\^</td></tr><tr><td>$</td><td>表示字符串的终止位置，如果想匹配’$’字符，则需要使用\$</td></tr><tr><td>\b</td><td>word边界，注意是不包含空格的，比如将(\bOK\b)应用到’It’s OK now’中，将只会匹配’OK’</td></tr><tr><td>\B</td><td>非word边界，与\b的作用相反。将’\Bon\B’应用到”Left on”将会失败，但应用到”tonne”则会成功</td></tr><tr><td>(?=E)</td><td>正向先行断言，比如，将’const(?=\s+char)’应用到’static const char *’中，将会匹配’const’。区别于’const\s+char’，将匹配’const char’</td></tr><tr><td>(?!E)</td><td>反向先行断言，比如，将’const(?=\s+char)’将匹配那些后面不接’char’的’const’</td></tr></tbody></table><h3 id="Wildcard-Matching（通配符匹配）"><a href="#Wildcard-Matching（通配符匹配）" class="headerlink" title="Wildcard Matching（通配符匹配）"></a>Wildcard Matching（通配符匹配）</h3><table><thead><tr><th>表达式</th><th>备注</th></tr></thead><tbody><tr><td>c</td><td>字符c</td></tr><tr><td>?</td><td>匹配任意单个字符，同正则表达式中的’.’作用一样</td></tr><tr><td>*</td><td>匹配0个或多个字符，同正则表达式中的’.*’作用一样</td></tr><tr><td>[…]</td><td>匹配任意单个字符，同正则表达式中的’.’作用一样</td></tr></tbody></table><p>使用exactMatch函数检测字符串是否符合通配符模式：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">QRegExp rx(&quot;*.txt&quot;);</span><br><span class="line">rx.setPatternSyntax(QRegExp::Wildcard);</span><br><span class="line">rx.exactMatch(&quot;README.txt&quot;);        // returns true</span><br><span class="line">rx.exactMatch(&quot;welcome.txt.bak&quot;);   // returns false</span><br></pre></td></tr></table></figure>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>OpenGl in Qt-显示三角形</title>
      <link href="/2019/11/30/OpenGl%20in%20Qt-%E6%98%BE%E7%A4%BA%E4%B8%89%E8%A7%92%E5%BD%A2/"/>
      <url>/2019/11/30/OpenGl%20in%20Qt-%E6%98%BE%E7%A4%BA%E4%B8%89%E8%A7%92%E5%BD%A2/</url>
      
        <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>很多人学习OpenGL，都是使用GLUT来创建小窗体，对于鼠标事件/键盘事件都是通过回调来实现，对于现代OpenGL来说，对着色器程序的创建和链接，这些工作都可以使用Qt框架来做，或者Qt其实已经帮我们封装好了类，我们不用重复造轮子（况且造的轮子还一样），其实可以省掉很多时间。</p><h2 id="基础类"><a href="#基础类" class="headerlink" title="基础类"></a>基础类</h2><h3 id="QOpenGLWidget"><a href="#QOpenGLWidget" class="headerlink" title="QOpenGLWidget"></a>QOpenGLWidget</h3><p>QOpenGLWidget是继承于QWidget，而QWidget是很多Qt中可视化控件的基类，QWidget实现了鼠标事件，键盘事件，重绘事件等，并且都是虚函数，所以子类只需要重载对应的函数即可。所以QOpenGLWidget继承于QWidget，就可以重载很多事件，比如，键盘方向键控制旋转等。<br>QOpenGLWidget还有三个重要的虚函数：initializeGL、paintGL、resizeGL函数。</p><p>initializeGL函数主要准备OpenGL要绘制的数据和初始化一些变量及状态，仅在第一次调用paintGL或resizeGL之前调用。</p><p>paintGL函数就是执行绘制的地方，所有的绘制代码都在这里，比如，清屏，画三角形，画贴图，等等。</p><p>resizeGL函数是窗口大小变化时会调用的，平时要对它做的操作比较少。</p><h3 id="QOpenGLFunctions"><a href="#QOpenGLFunctions" class="headerlink" title="QOpenGLFunctions"></a>QOpenGLFunctions</h3><p>QOpenGLFunctions是封装了一个OpenGL ES 2.0 API的类，可以说使用了设计模式里面的适配器模式，通过它，可以使用与原生OpenGL API一致的函数。当然还有其它一些功能，比如查询是否支持某个特效等，不过相对用的比较少。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2019/11/24/hello-world/"/>
      <url>/2019/11/24/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
